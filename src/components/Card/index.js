import React from 'react';
// import PropTypes from 'prop-types';

import './Card.css';


const Card = () => {
  return (
    <div className={'cardWrapper'}>
      {'card'}
    </div>
  );
}

// Card.propTypes = {
// };

export default Card;
