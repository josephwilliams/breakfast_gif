import { combineReducers } from 'redux';

import giphyReducer from 'redux/reducers/giphy.js';


// TODO: add Giphy and other reducers (navigation?).
export default combineReducers({
  giphyReducer,
});
